extends ../_shared/_master

block content
    ul
        each todo in todos
            li 
                a(href="/todos/" + todo._id) #{todo.text}
                input(type="checkbox" checked=todo.isDone)
                ="(" + todo.category + ")"